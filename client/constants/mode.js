import * as Strings from './string';

export const AUTO = 'automatic';
export const SCHED = 'scheduled';
export const MANUAL = 'manual';

export const modeTitles = {
    'automatic': Strings.AUTOMATIC,
    'scheduled': Strings.SCHEDULED,
    'manual': Strings.MANUAL
}